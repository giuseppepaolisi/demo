pipeline {
    agent any
    stages {
            stage('Checkout') {
                steps {
                    sh 'echo "Checking out code..."'
                    checkout scm
                }
            }
    }
}