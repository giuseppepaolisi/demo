pipeline {
    agent any
    stages {
            stage('Build') {
                steps {
                    sh "echo 'Current workspace is $WORKSPACE'"
                    sh 'echo "*********Build successful!"'
                }

            }
    }
}