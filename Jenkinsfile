pipeline {
    agent any
    stages {
        Stage('Build') {
            stage('Checkout') {
            steps {
                sh 'echo "Checking out code..."'
            }
        }
        }
    }
}